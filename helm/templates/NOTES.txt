Deployed agents:
{{- range $agentId, $agent := .Values.agents }}
  - {{ $agent.name }} ({{ $agentId }}){{- if $agent.server }}{{- if $agent.server.url }} - {{ $agent.server.url }}{{- end }}{{- end }}
{{- end }}

{{- if .Values.mcpServers }}

Deployed MCP servers:
{{- range .Values.mcpServers }}
  - {{ .id }}
{{- end }}
{{- end }}

{{- if .Values.slackBot.enabled }}

Slack Bot:
  - Connected to agent: {{ index $.Values.agents .Values.slackBot.a2a.agentId "name" | default .Values.slackBot.a2a.agentId | default "external" }}
{{- end }}
