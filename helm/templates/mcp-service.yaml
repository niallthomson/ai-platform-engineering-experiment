{{- range .Values.mcpServers }}
{{- $mcpServer := . }}
{{- $ctx := dict "root" $ "mcpServer" $mcpServer }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-ai.mcpServerFullname" $ctx }}
  labels:
    {{- include "platform-ai.labels" $ | nindent 4 }}
    app.kubernetes.io/type: mcp-server
    app.kubernetes.io/component: {{ $mcpServer.id }}
spec:
  type: {{ $mcpServer.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-ai.mcpServerSelectorLabels" $ctx | nindent 4 }}
{{- end }}
